-- [[ LuckyHub Key System UI ]] --
-- Разработчик: Сгенерировано профессионально для вас (Perfect Clipping Update)

local LocalPlayer = game:GetService("Players").LocalPlayer
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")

-- [[ НАСТРОЙКИ ]] --
local BLUR_INTENSITY = 20 
local TWEEN_SPEED = 0.5 

-- Удаляем старый интерфейс и старый Blur, если они существовали
if CoreGui:FindFirstChild("LuckyHub_KeySystem") then
    CoreGui["LuckyHub_KeySystem"]:Destroy()
end
if Lighting:FindFirstChild("LuckyHub_Blur") then
    Lighting["LuckyHub_Blur"]:Destroy()
end

-- Создание ScreenGui
local LuckyHub_KeySystem = Instance.new("ScreenGui")
LuckyHub_KeySystem.Name = "LuckyHub_KeySystem"
LuckyHub_KeySystem.Parent = CoreGui
LuckyHub_KeySystem.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Логика Blur
local Blur = Instance.new("BlurEffect")
Blur.Name = "LuckyHub_Blur"
Blur.Parent = Lighting
Blur.Size = 0 

TweenService:Create(Blur, TweenInfo.new(TWEEN_SPEED, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = BLUR_INTENSITY}):Play()

local function removeBlur()
    local tween = TweenService:Create(Blur, TweenInfo.new(TWEEN_SPEED, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = 0})
    tween:Play()
    tween.Completed:Connect(function()
        Blur:Destroy()
    end)
end

-- Главное окно (MainFrame)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = LuckyHub_KeySystem
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25) 
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.Size = UDim2.new(0, 320, 0, 240) 
MainFrame.BorderSizePixel = 0
MainFrame.Active = true 

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(45, 45, 45)
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

-- ==========================================
-- ИСПРАВЛЕННЫЙ КОНТЕЙНЕР ДЛЯ ЧАСТИЦ
-- ==========================================
local ParticleContainer = Instance.new("CanvasGroup")
ParticleContainer.Name = "ParticleContainer"
ParticleContainer.Size = UDim2.new(1, 0, 1, 0)
ParticleContainer.BackgroundTransparency = 1
ParticleContainer.ZIndex = 1
ParticleContainer.Parent = MainFrame

-- Округляем контейнер частиц, чтобы они не вылетали даже на углах
local ParticleCorner = Instance.new("UICorner")
ParticleCorner.CornerRadius = UDim.new(0, 12)
ParticleCorner.Parent = ParticleContainer

-- Заголовок (Title)
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0, 15)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "LuckyHub"
Title.TextColor3 = Color3.fromRGB(0, 255, 127) 
Title.TextSize = 22.000
Title.ZIndex = 3 

-- Текстовое поле для ввода (TextBox)
local KeyInput = Instance.new("TextBox")
KeyInput.Name = "KeyInput"
KeyInput.Parent = MainFrame
KeyInput.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
KeyInput.Position = UDim2.new(0.5, -120, 0, 75)
KeyInput.Size = UDim2.new(0, 240, 0, 40)
KeyInput.Font = Enum.Font.Gotham
KeyInput.PlaceholderText = "Enter your key"
KeyInput.Text = ""
KeyInput.PlaceholderColor3 = Color3.fromRGB(120, 120, 120)
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.TextSize = 14.000
KeyInput.ZIndex = 3

local KeyInputCorner = Instance.new("UICorner")
KeyInputCorner.CornerRadius = UDim.new(0, 8)
KeyInputCorner.Parent = KeyInput

local KeyInputStroke = Instance.new("UIStroke")
KeyInputStroke.Color = Color3.fromRGB(50, 50, 50)
KeyInputStroke.Thickness = 1
KeyInputStroke.Parent = KeyInput

-- Кнопка проверки ключа (Enter Key)
local EnterKeyBtn = Instance.new("TextButton")
EnterKeyBtn.Name = "EnterKeyBtn"
EnterKeyBtn.Parent = MainFrame
EnterKeyBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255) 
EnterKeyBtn.Position = UDim2.new(0.5, -120, 0, 130)
EnterKeyBtn.Size = UDim2.new(0, 240, 0, 40)
EnterKeyBtn.Font = Enum.Font.GothamBold
EnterKeyBtn.Text = "Enter Key"
EnterKeyBtn.TextColor3 = Color3.fromRGB(0, 0, 0) 
EnterKeyBtn.TextSize = 14.000
EnterKeyBtn.AutoButtonColor = false
EnterKeyBtn.ZIndex = 3

local EnterKeyCorner = Instance.new("UICorner")
EnterKeyCorner.CornerRadius = UDim.new(0, 8)
EnterKeyCorner.Parent = EnterKeyBtn

-- Кнопка получения ключа (Get Key)
local GetKeyBtn = Instance.new("TextButton")
GetKeyBtn.Name = "GetKeyBtn"
GetKeyBtn.Parent = MainFrame
GetKeyBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255) 
GetKeyBtn.Position = UDim2.new(0.5, -120, 0, 176) 
GetKeyBtn.Size = UDim2.new(0, 240, 0, 40)
GetKeyBtn.Font = Enum.Font.GothamBold
GetKeyBtn.Text = "Get Key"
GetKeyBtn.TextColor3 = Color3.fromRGB(0, 0, 0) 
GetKeyBtn.TextSize = 14.000
GetKeyBtn.AutoButtonColor = false
GetKeyBtn.ZIndex = 3

local GetKeyCorner = Instance.new("UICorner")
GetKeyCorner.CornerRadius = UDim.new(0, 8)
GetKeyCorner.Parent = GetKeyBtn

-- ==========================================
-- СИСТЕМА ПАДАЮЩИХ КЛЕВЕРОВ
-- ==========================================
task.spawn(function()
    while LuckyHub_KeySystem and LuckyHub_KeySystem.Parent do
        task.wait(0.25) 
        if not ParticleContainer or not ParticleContainer.Parent then break end
        
        local Clover = Instance.new("TextLabel")
        Clover.Name = "Clover"
        Clover.Parent = ParticleContainer -- Теперь спавнятся строго КАНВАСЕ
        Clover.BackgroundTransparency = 1
        Clover.Size = UDim2.new(0, 20, 0, 20)
        
        Clover.Position = UDim2.new(math.random(0, 100) / 100, -10, 0, -20)
        Clover.Font = Enum.Font.Gotham
        Clover.Text = math.random(1, 2) == 1 and "🍀" or "☘️" 
        Clover.TextSize = math.random(12, 18) 
        Clover.TextTransparency = math.random(4, 7) / 10 
        Clover.ZIndex = 1 
        
        local fallDuration = math.random(25, 45) / 10 
        local driftX = Clover.Position.X.Scale + (math.random(-15, 15) / 100) 
        local rotation = math.random(-360, 360) 
        
        local tweenInfo = TweenInfo.new(fallDuration, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
        local tween = TweenService:Create(Clover, tweenInfo, {
            Position = UDim2.new(driftX, 0, 1, 20),
            Rotation = rotation
        })
        
        tween:Play()
        
        tween.Completed:Connect(function()
            Clover:Destroy()
        end)
    end
end)

-- ==========================================
-- ВНУТРЕННЯЯ ЛОГИКА UI
-- ==========================================

-- 1. Скрипт перетаскивания (Smooth Drag System)
local dragging, dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    local targetPos = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    TweenService:Create(MainFrame, TweenInfo.new(0.08, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = targetPos}):Play()
end

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- 2. Анимации при наведении (Hover Effects)
local function createHoverAnimation(button, targetColor, duration)
    local originalColor = button.BackgroundColor3
    button.MouseEnter:Connect(function()
        TweenService:Create(button, TweenInfo.new(duration, Enum.EasingStyle.Quad), {BackgroundColor3 = targetColor}):Play()
    end)
    button.MouseLeave:Connect(function()
        TweenService:Create(button, TweenInfo.new(duration, Enum.EasingStyle.Quad), {BackgroundColor3 = originalColor}):Play()
    end)
end

createHoverAnimation(EnterKeyBtn, Color3.fromRGB(230, 230, 230), 0.2)
createHoverAnimation(GetKeyBtn, Color3.fromRGB(230, 230, 230), 0.2)

-- 3. Функционал кнопки "Get Key"
GetKeyBtn.MouseButton1Click:Connect(function()
    local keyLink = "https://link-to-your-key.com"
    
    if setclipboard then
        setclipboard(keyLink)
    elseif toclipboard then
        toclipboard(keyLink)
    end
    
    GetKeyBtn.Text = "Link Copied!"
    GetKeyBtn.TextColor3 = Color3.fromRGB(0, 180, 90) 
    
    task.wait(2)
    
    GetKeyBtn.Text = "Get Key"
    GetKeyBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
end)

-- 4. Логика кнопки "Enter Key"
EnterKeyBtn.MouseButton1Click:Connect(function()
    local enteredKey = KeyInput.Text
    local correctKey = "LuckyHub_Pro_2026" 
    
    if enteredKey == correctKey then
        Title.Text = "Access Granted!"
        Title.TextColor3 = Color3.fromRGB(0, 255, 127)
        
        removeBlur()
        
        task.wait(1)
        LuckyHub_KeySystem:Destroy()
        print("LuckyHub успешно загружен!")
    else
        KeyInput.Text = ""
        KeyInput.PlaceholderText = "Invalid Key! Try again."
        KeyInputStroke.Color = Color3.fromRGB(255, 64, 64)
        
        task.wait(2)
        
        KeyInput.PlaceholderText = "Enter your key"
        KeyInputStroke.Color = Color3.fromRGB(50, 50, 50)
    end
end)
